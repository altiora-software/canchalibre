# Invariantes de datos

Leer `../../../../docs/agent-context.md`, `backend-data` y el nodo crítico afectado. Roles sólo por backend autorizado; reservas sólo por `create_reservation`; valores de precio/pago no se confían al cliente; toda política debe respetar mínimo privilegio y toda migración se prueba en staging.
