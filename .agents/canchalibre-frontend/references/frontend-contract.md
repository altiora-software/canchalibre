# Contexto frontend

Leer `../../../../docs/agent-context.md`, el nodo `frontend` y el contrato BFF enlazado. Para rutas, leer `src/App.tsx`; para un flujo, abrir sólo su página/componente/hook y los cambios relacionados. Reservas, roles y pagos se delegan a contratos server-side.
